secrets:
  mysql_root_password:
    file: ./secrets/mysql_root_password
  mysql_password:
    file: ./secrets/mysql_password
  pyth_api_key:
    file: ./secrets/pyth_api_key
  resend_api_key:
    file: ./secrets/resend_api_key
  telegram_bot_token:
    file: ./secrets/telegram_bot_token
  eth_rpc_url:
    file: ./secrets/eth_rpc_url
  base_rpc_url:
    file: ./secrets/base_rpc_url
  arb_rpc_url:
    file: ./secrets/arb_rpc_url
  solana_rpc_url:
    file: ./secrets/solana_rpc_url

services:
  kafka:
    image: apache/kafka:3.9.2
    container_name: crypto-alert-kafka
    networks:
      - crypto-alert-net
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-consumer-groups.sh --bootstrap-server localhost:9092 --list >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 15
      start_period: 30s
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: crypto-alert-mysql
    networks:
      - crypto-alert-net
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/mysql_root_password
      MYSQL_DATABASE: ${MYSQL_DB:-web3}
      MYSQL_USER: ${MYSQL_USER:-cryptoalert}
      MYSQL_PASSWORD_FILE: /run/secrets/mysql_password
    secrets:
      - mysql_root_password
      - mysql_password
    volumes:
      - mysql-data:/var/lib/mysql
      - ./sql/alert_rules_schema.sql:/docker-entrypoint-initdb.d/01_alert_rules_schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "--silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  es01:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.3.0
    container_name: es01
    networks:
      - crypto-alert-net
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\"\\|\"status\":\"yellow\"' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1GB
    restart: unless-stopped

  log-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: crypto-alert:latest
    container_name: log-api
    command: ["./log-api"]
    networks:
      - crypto-alert-net
    ports:
      - "127.0.0.1:8181:8181"
    environment:
      API_PORT: "8181"
      LOG_DIR: /app/logs
      ES_ENABLED: "true"
      ES_ADDRESSES: http://es01:9200
      ES_INDEX: crypto-alert-logs
      MYSQL_USER: ${MYSQL_USER:-cryptoalert}
      MYSQL_DB: ${MYSQL_DB:-web3}
    secrets:
      - mysql_password
    volumes:
      - log-data:/app/logs
    depends_on:
      mysql:
        condition: service_healthy
      es01:
        condition: service_healthy
    restart: unless-stopped

  crypto-alert:
    build:
      context: .
      dockerfile: Dockerfile
    image: crypto-alert:latest
    container_name: crypto-alert
    command: ["./crypto-alert"]
    networks:
      - crypto-alert-net
    environment:
      LOG_DIR: /app/logs
      ES_ENABLED: "true"
      ES_ADDRESSES: http://es01:9200
      ES_INDEX: crypto-alert-logs
      KAFKA_BROKERS: kafka:9092
      MYSQL_USER: ${MYSQL_USER:-cryptoalert}
      MYSQL_DB: ${MYSQL_DB:-web3}
      PYTH_API_URL: ${PYTH_API_URL:-https://hermes.pyth.network}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-}
      CHECK_INTERVAL: ${CHECK_INTERVAL:-60}
    secrets:
      - mysql_password
      - pyth_api_key
      - resend_api_key
      - telegram_bot_token
      - eth_rpc_url
      - base_rpc_url
      - arb_rpc_url
      - solana_rpc_url
    volumes:
      - log-data:/app/logs
    depends_on:
      mysql:
        condition: service_healthy
      es01:
        condition: service_healthy
      kafka:
        condition: service_healthy
    restart: unless-stopped

  notification-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: crypto-alert:latest
    container_name: notification-service
    command: ["./notification-service"]
    networks:
      - crypto-alert-net
    environment:
      KAFKA_BROKERS: kafka:9092
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-}
    secrets:
      - resend_api_key
      - telegram_bot_token
    depends_on:
      kafka:
        condition: service_healthy
    restart: unless-stopped
    stop_grace_period: 5s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: crypto-alert-frontend
    networks:
      - crypto-alert-net
    ports:
      - "3030:3030"
    environment:
      BACKEND_URL: http://log-api:8181
    depends_on:
      - log-api
    restart: unless-stopped

networks:
  crypto-alert-net:
    driver: bridge

volumes:
  log-data:
  mysql-data:
